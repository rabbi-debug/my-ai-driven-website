\# Codebase Rules for Claude

\- Architecture: Modular HTML/JavaScript component layouts.

\- Component items live exclusively inside `src/components/`.

\- Page structures live exclusively inside `src/pages/`.

\- Workflow Safety: Never commit code directly to `main`. Always execute a feature branch workflow first.

